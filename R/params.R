#' Parameters
#'
#' @name params
#' @keywords internal
#'
#' @param dir `string`. Output directory.
#' @param file `string`. Annotation file name.
#' @param gene `string`. Gene identifier.
#' @param genes `character`. Gene identifiers.
#' @param progress `boolean`. Show progress using pbapply package.
#' @param version `string` or `NULL`. WormBase version. If `NULL` (recommended),
#'   defaults to current production release version available on the WormBase
#'   website. Legacy versions can be specified as a `string` (e.g. "WS262").
NULL
