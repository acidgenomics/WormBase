# R for travis
# https://docs.travis-ci.com/user/languages/r

language: r
sudo: false
cache: packages
warnings_are_errors: true

r:
  - bioc-release
  - bioc-devel
r_packages:
  - covr

after_success:
  - Rscript -e 'covr::codecov()'
